# sanparks-watch: delivery of 20 September 2026

Nossob session. Four files changed, one of them in `.github/workflows/`.
`watch.py` is NOT in this zip and is NOT changed: `is_hit` is untouched, this
is a threshold change only.

## 1. `cameras.py` — Nossob DAYLIGHT only, two gates

    NB_MAX     25   ->  250
    FILL_CMP   0.40 ->  0.30

Nothing else moves. `thr_night` at Nossob sets `NB_MAX`, `FILL_CMP`,
`DIST_MAX` and `FILL_WIDE` explicitly, so this cannot reach Nossob night.
Talamati and Satara are untouched in both modes.

This is the one-change-per-session exception invoked for a second time, on the
same pair and the same mechanism as 4 September 2026 at Kruger daylight. The
numbers that justify it, measured 20 September 2026 by replaying the live
`is_hit` over `logs/nossob/` of 30 August to 18 September, 14,139 daylight
rows, against the refreshed label files (29 daylight priority frames, 863
eye-confirmed daylight empties):

    gates                 priority   empties   hits/day
    live                     6/29     142/863      8.9
    NB_MAX 250 alone         8/29     172/863     43.6
    FILL_CMP 0.30 alone      6/29     142/863     11.7
    both                    11/29     207/863     77.5

`FILL_CMP` 0.30 alone moves nothing at all. That is the inseparability
argument, and it is why the pair ships as one change.

DELIBERATELY NOT SHIPPED: `DIST_MAX` 8.0 and `FILL_WIDE` 0.26. Added on top
they give 18/29 for 375/863 and 219.7 hits a day: 7 more priority frames for
168 more leaked empties. Each is inert alone. Price them separately against the
`collect=all` windows of 18 September, which are the only Nossob sample not
selected by a gate.

## 2. `selftest.py` — three caps moved, all with the rows named in the file

    FP_MAX[("nossob","day")]                       0 -> 3
    REAL_MIN[("nossob","day")]                     2 -> 3
    EXPECT_FIELD[("nossob","day","gemsbok...")]    0 -> 8

BOTH COUNTS, before and after, Nossob daylight:

    naturals leaked            0/18  ->  0/18   (cap 0, unchanged)
    confirmed empties leaked    0/4  ->   3/4   (cap 0 -> 3)
    confirmed animals detected  2/5  ->   3/5   (floor 2 -> 3)
    injected gemsbok, FIELD     0/8  ->   8/8   (floor 0 -> 8)
    injected jackal,  FIELD     0/8  ->   0/8   (floor 0, unchanged)

The three leaks are the 31 August dawn frames at 06:48, 07:18 and 07:38 local,
named individually in the file with their rows. They are the frames `NB_MAX` 25
was fitted to reject, so this is the gate being reversed knowingly, not a
surprise. The fourth dawn frame still fails. Every other camera and mode is
byte-for-byte the same verdict.

Suite exits 0.

## 3. `.github/workflows/draw_boxes.yml` — one line of text

The `roots` input description said "hits, frames, or both". The script splits
on commas and uses each piece as a directory name, so `both` becomes
`both/<cam>`, every camera is skipped and the day filters are never reached.
Description corrected. No behaviour change. This was open item 15 and was held
back to travel with a code delivery rather than be pushed alone.

## 4. `tools/reviewed.txt`, `tools/ground_truth.txt`, `tools/priority.txt`

The Nossob review pass of 8 to 18 September 2026, appended as a dated block to
each file. Nossob only; Talamati and Satara stay frozen at 7 September.

    reviewed.txt      4,109 -> 5,630   (+1,521)
    ground_truth.txt    524 -> 1,182   (+658)
    priority.txt        223 ->   682   (+459)

The reviewed keys were regenerated from the repo tarball, not typed, by
reproducing each pass's own filter: 751 hits for Pass B1, 352 drawn for Pass A
at `--md-min 0.2` of which 307 are distinct instants, and 463 keys for Pass C
over 17 and 18 September. The negative set goes from 3,585 to 5,038
eye-confirmed empties, 863 of them Nossob daylight against 499 before.

## What is NOT in this zip and why

- `watch.py`: unchanged. No `is_hit` edit, no transport edit.
- `POLL` 10 -> 20 s (open item 13): still gated on reading the session-drop
  rule against `logs/http/`, which has not been read. Both move the same
  counter, so shipping them together would confound the only clean measurement.
- `selftest.py`'s negative sets rebuilt from the label files (open item 9):
  the suite still carries 18 synthetic Nossob daylight naturals and 4 confirmed
  empties against 863 real ones that now exist. That is a session of its own and
  it is now the harness's weakest point.
